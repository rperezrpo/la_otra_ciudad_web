# ─────────────────────────────────────────────────────────────────────────────
# Cada variable lleva marcado dónde tiene que existir:
#
#   [VERCEL] la usan las rutas /api/* al ejecutarse → DEBE estar también en
#            Vercel (Settings → Environment Variables), no solo aquí. Añadirla
#            no basta: hay que redesplegar para que entre en vigor.
#   [LOCAL]  solo la usan los scripts de scripts/, que corren en tu máquina.
#
# Comprueba que no falta ninguna con:  npm run check:env
# ─────────────────────────────────────────────────────────────────────────────

# [LOCAL] Sanity — identificadores públicos. El sitio los tiene fijos en
# astro.config.mjs; aquí solo hacen falta para los scripts. Sin comillas ni
# espacios: un valor sucio rompe createClient al importarse.
PUBLIC_SANITY_PROJECT_ID=your_project_id
PUBLIC_SANITY_DATASET=production

# [VERCEL] [LOCAL] Sanity — solo servidor. Da acceso de escritura al dataset:
# lo usan los editores de /editar y los scripts de importación.
# Create at https://www.sanity.io/manage → API → Tokens (Editor permission).
# Ojo: permiso Editor, no Viewer — Viewer lee pero no escribe.
# NEVER commit this value.
SANITY_WRITE_TOKEN=your_write_token

# [VERCEL] Google sign-in (Auth.js) — powers the team self-service page /equipo/editar.
# 1. Create OAuth credentials at https://console.cloud.google.com/apis/credentials
#    (consent screen → "Internal" so only your Workspace can sign in).
# 2. Authorized redirect URIs:
#    - http://localhost:4321/api/auth/callback/google   (local dev)
#    - https://laotraciudad.org/api/auth/callback/google (production)
# AUTH_SECRET: random string, e.g. `openssl rand -base64 32`. NEVER commit these.
AUTH_SECRET=your_random_secret
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# [VERCEL] Resend — server-side only. Powers the /api/contact form endpoint.
# Create at https://resend.com → API Keys. NEVER commit this value.
RESEND_API_KEY=your_resend_api_key
# Optional overrides (defaults shown). FROM must be on a domain you verified in Resend.
CONTACT_TO_EMAIL=info@laotraciudad.org
CONTACT_FROM_EMAIL=web@laotraciudad.org

# [VERCEL] Wompi (Bancolombia) — powers the /api/donate donation flow.
# Get both at https://comercios.wompi.co → Desarrolladores.
#
# Production keys (used when WOMPI_PUBLICA is set).
WOMPI_PUBLICA=pub_prod_xxxxxxxxxxxxxxxxxxxx
WOMPI_INTEGRITY=prod_integrity_xxxxxxxxxxxxxxxxxxxx
#
# Test keys (fallback when prod keys are absent — use for local dev).
WOMPI_PUBLIC_TEST=pub_test_xxxxxxxxxxxxxxxxxxxx
WOMPI_INTEGRITY_TEST=test_integrity_xxxxxxxxxxxxxxxxxxxx
