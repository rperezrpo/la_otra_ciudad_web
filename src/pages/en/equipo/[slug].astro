---
import PersonaDetalleBody from '../../../components/pages/PersonaDetalleBody.astro';
import { personStaticPaths } from '../../../lib/personPaths';

export async function getStaticPaths() {
  return personStaticPaths('en');
}

const { person } = Astro.props;
---

<PersonaDetalleBody person={person} />
