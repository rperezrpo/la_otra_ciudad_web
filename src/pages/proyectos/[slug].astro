---
import ProyectoDetalleBody from '../../components/pages/ProyectoDetalleBody.astro';
import { projectStaticPaths } from '../../lib/projectPaths';

export async function getStaticPaths() {
  return projectStaticPaths('es');
}

const { project } = Astro.props;
---

<ProyectoDetalleBody project={project} />
