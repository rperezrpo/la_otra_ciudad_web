---
import { pageLang, useTranslations, localizePath } from '../../i18n/ui';

const lang = pageLang(Astro.currentLocale);
const t = useTranslations(lang);
const l = (path: string) => localizePath(lang, path);

const columns = [
  {
    heading: t('footer.organizacion'),
    links: [
      { label: t('nav.quienesSomos'), href: l('/quienes-somos') },
      { label: t('nav.proyectos'),     href: l('/proyectos') },
      { label: t('nav.metodologia'),   href: l('/metodologia') },
      { label: t('nav.servicios'),     href: l('/servicios') },
    ],
  },
  {
    heading: t('footer.involucrate'),
    links: [
      { label: t('nav.contacto'),     href: l('/contacto') },
      { label: t('nav.donarAria'),    href: l('/donar') },
    ],
  },
];
---

<footer style="background: #1a1410; color: #fff; padding: 64px 48px 40px; font-family: var(--font-sans);">
  <div style="max-width: 1100px; margin: 0 auto;">

    <!-- Top row: brand + columns -->
    <div style="display: flex; gap: 64px; margin-bottom: 64px; flex-wrap: wrap;">

      <!-- Brand column -->
      <div style="flex: 1.5; min-width: 240px;">
        <img
          src="/images/logo/logo_completo_cuadrado_blanco.svg"
          alt="La Otra Ciudad"
          loading="lazy"
          style="height: 36px; width: auto; margin-bottom: 16px;"
          onerror="this.style.display='none'; this.nextElementSibling.style.display='block';"
        />
        <div style="display:none; font-size: 17px; font-weight: 600; letter-spacing: -0.3px; margin-bottom: 16px;">La Otra Ciudad</div>
        <p style="font-size: 15px; font-weight: 300; line-height: 1.6; color: rgba(255,255,255,0.5); max-width: 280px;">
          {t('footer.tagline')}
        </p>

        <!-- Social links -->
        <div style="margin-top: 28px; display: flex; gap: 10px;">
          <a href="https://www.instagram.com/laotraciudadmed/" aria-label="Instagram de La Otra Ciudad" target="_blank" rel="noopener" style="width:36px;height:36px;border-radius:50%;background:rgba(255,255,255,0.08);display:flex;align-items:center;justify-content:center;color:rgba(255,255,255,0.5);transition:background 150ms,color 150ms;" onmouseover="this.style.background='rgba(255,255,255,0.18)';this.style.color='#fff'" onmouseout="this.style.background='rgba(255,255,255,0.08)';this.style.color='rgba(255,255,255,0.5)'">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="2" y="2" width="20" height="20" rx="5"/><circle cx="12" cy="12" r="4"/><circle cx="17.5" cy="6.5" r="1" fill="currentColor" stroke="none"/></svg>
          </a>
          <a href="https://www.linkedin.com/company/laotraciudad" aria-label="LinkedIn de La Otra Ciudad" target="_blank" rel="noopener" style="width:36px;height:36px;border-radius:50%;background:rgba(255,255,255,0.08);display:flex;align-items:center;justify-content:center;color:rgba(255,255,255,0.5);transition:background 150ms,color 150ms;" onmouseover="this.style.background='rgba(255,255,255,0.18)';this.style.color='#fff'" onmouseout="this.style.background='rgba(255,255,255,0.08)';this.style.color='rgba(255,255,255,0.5)'">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
          </a>
        </div>
      </div>

      <!-- Nav columns -->
      {columns.map((col) => (
        <div style="flex: 1; min-width: 160px;">
          <div style="font-family: var(--font-mono); font-size: 11px; letter-spacing: 0.54px; text-transform: uppercase; color: rgba(255,255,255,0.35); margin-bottom: 16px;">
            {col.heading}
          </div>
          <ul style="list-style: none; display: flex; flex-direction: column; gap: 10px;">
            {col.links.map((link) => (
              <li>
                <a
                  href={link.href}
                  style="font-size: 14px; font-weight: 300; color: rgba(255,255,255,0.65); letter-spacing: -0.1px; text-decoration: none; transition: color 150ms;"
                  onmouseover="this.style.color='#fff'"
                  onmouseout="this.style.color='rgba(255,255,255,0.65)'"
                >
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>

    <!-- Bottom row: copyright -->
    <div style="border-top: 1px solid rgba(255,255,255,0.08); padding-top: 24px; display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 12px;">
      <span style="font-size: 13px; color: rgba(255,255,255,0.3);">
        {t('footer.copyright')}
      </span>
      <span style="font-family: var(--font-mono); font-size: 11px; color: rgba(255,255,255,0.25); letter-spacing: 0.4px;">
        NIT 901.889.218
      </span>
    </div>

  </div>
</footer>
